# Leitor de Placas com YOLO + OCR

Este projeto utiliza (temporariamente) **Streamlit** para criar uma interface web que permite enviar imagens e detectar placas de veículos usando **YOLOv8** e **EasyOCR**.

## Executar localmente

```bash
pip install -r requirements.txt
streamlit run app.py
